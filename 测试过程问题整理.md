# HuaweiCloud Cilium 测试过程问题整理

本文整理 HuaweiCloud SubENI 适配在配置、云 API、IPAM、并发调谐、策略路由、BPF
和测试工具方面暴露的问题。内容只保留可复用的问题描述、根因和解决方案，不包含环境或
认证信息。

## 一、客户配置与 Service 语义

### 1. CNI `min-allocate` 未传播到 CiliumNode

* **实际测试问题场景：** CNI 配置了 `min-allocate`，但节点侧 CiliumNode 仍使用默认水位，预分配行为与配置不一致。
* **根因：** Agent 读取 CNI 配置后，没有把 `min-allocate` 合并到 HuaweiCloud 节点发现和 CiliumNode 更新路径。
* **解决方案：** 在 CNI 配置合并阶段保留该字段，并在节点发现时写入 CiliumNode；Agent 配置继续保持更高优先级。

### 2. Probe 模式覆盖显式关闭的 NodePort

* **实际测试问题场景：** 同时配置 `kube-proxy-replacement=probe` 和 `enable-node-port=false` 时，BPF NodePort 仍被自动开启。
* **根因：** Probe 逻辑把能力探测结果直接写回 NodePort 开关，没有区分用户显式配置和默认值。
* **解决方案：** 保留显式 `false` 的优先级，仅在用户未明确关闭时根据探测结果启用 NodePort。

### 3. 关闭 NodePort 后 Socket LB 仍然启用

* **实际测试问题场景：** NodePort 已由 kube-proxy 接管，但 Socket LB 仍处理 Service 流量，形成两套数据面并存。
* **根因：** NodePort 和 Socket LB 的开关计算相互独立，Probe 分支只关闭了部分能力。
* **解决方案：** 当 Probe 模式确认使用 kube-proxy 且 NodePort 显式关闭时，同步关闭 Socket LB。

### 4. Host 访问本机 Pod 的源地址不符合预期

* **实际测试问题场景：** 宿主机访问本机 SubENI Pod 时，Pod 观察到的源地址是宿主机主网卡地址，而不是 `cilium_host` 地址。
* **根因：** HuaweiCloud endpoint route 缺少显式源地址，Linux 根据主路由自动选择了宿主机主地址。
* **解决方案：** 创建 endpoint route 时，将路由源地址设置为 `cilium_host` 地址。

### 5. 本地 Service Hairpin 的 SNAT 语义不一致

* **实际测试问题场景：** Pod 通过 ClusterIP 或 NodePort 访问自身及本机后端时，后端看到的源地址与客户基线不一致。
* **根因：** 通用本地 Service 规则没有区分 Pod hairpin、Host DNAT 和 HuaweiCloud SubENI 路径。
* **解决方案：** 增加 HuaweiCloud 专用 SNAT 规则，分别处理 Pod hairpin 和 Host 到本机后端的流量。

### 6. `cilium_host` 到 VPC 的主路由不完整

* **实际测试问题场景：** 部分 Host、Service 和 Pod 回程流量无法稳定选择 `cilium_host` 作为正确出口。
* **根因：** 路由初始化只覆盖 endpoint 专用表，遗漏了 `cilium_host` 到 VPC 的主路由约束。
* **解决方案：** 补齐 `cilium_host` 到 VPC CIDR 的主路由，并与 endpoint 专用策略路由保持一致。

### 7. 本机 NodePort 到远端后端错误改写源地址

* **实际测试问题场景：** 本机 Pod 访问本机 NodePort、后端位于其他节点时，远端后端看到的源地址被改写。
* **根因：** 本地 Service SNAT 规则范围过宽，把应保留 Pod 源地址的跨节点路径也纳入处理。
* **解决方案：** 收紧匹配条件，仅对真正需要 hairpin 或本地 DNAT 的路径执行 SNAT。

### 8. kube-proxy `clusterCIDR` 与 SubENI 地址范围不一致

* **实际测试问题场景：** kube-proxy 仍使用传统 PodCIDR，而 SubENI Pod 使用 VPC 地址，导致流量分类和源地址处理异常。
* **根因：** 部署配置沿用了非 ENI 网络模型的默认 CIDR。
* **解决方案：** 将 kube-proxy `clusterCIDR` 配置为实际承载 SubENI Pod 的 VPC CIDR。

## 二、Metadata、云 API 与 IPAM

### 9. Metadata 链路顺序变化导致 trunk 识别错误

* **实际测试问题场景：** Metadata 中第一条 link 为空、顺序变化或存在多网卡时，Agent 可能选择错误的 trunk。
* **根因：** 旧逻辑依赖固定数组位置，没有按有效 port ID、MAC 和本机接口进行匹配。
* **解决方案：** 跳过空链路，按 MAC 和 port ID 匹配本机接口；多网卡环境要求显式确认 trunk。

### 10. 可用区和 Region 推导过于宽松

* **实际测试问题场景：** 空可用区、单字符值或非标准后缀可能被解析为看似有效的 Region。
* **根因：** Region 通过简单截断字符串获得，没有校验可用区格式。
* **解决方案：** 只接受符合约束的可用区后缀，无法可靠推导时立即返回配置错误。

### 11. 云 API 分页可能死循环或发布不完整快照

* **实际测试问题场景：** API 返回重复 marker、空页但仍有 next marker、重复 ID 或无进展满页时，列表同步无法可靠结束。
* **根因：** 分页循环只判断 next marker 是否为空，没有记录历史 marker 和资源唯一性。
* **解决方案：** 检查 marker 单调前进，拒绝重复 marker、空 ID 和重复资源；异常分页不发布部分结果。

### 12. 子网容量双 API 合并存在不一致窗口

* **实际测试问题场景：** 容量接口和 Virsubnet 接口返回的子网集合不一致时，可能把容量未知的子网当作可分配候选。
* **根因：** 两个 API 的结果按宽松关联方式合并，缺少缺失项和重复项校验。
* **解决方案：** 以 Virsubnet 作为资源基准，要求每个候选都具有唯一、有效的容量记录；孤立容量记录只忽略，不参与选择。

### 13. 子网候选选择未严格区分容量不足和 API 错误

* **实际测试问题场景：** 首个显式子网发生权限、网络或服务端错误时，逻辑可能继续尝试后续子网，掩盖真实故障。
* **根因：** 所有错误都被当成“当前子网不可用”。
* **解决方案：** 只有明确的容量不足允许选择下一个候选；鉴权、网络和服务端错误必须原样返回。

### 14. SubENI 标签接口在部分 Region 不可用

* **实际测试问题场景：** SubENI 创建成功后，再调用独立 Port 标签接口会返回接口不存在，导致整个创建流程回滚。
* **根因：** 实现假设所有 Region 都发布了相同的 Port 标签 API。
* **解决方案：** 在单个和批量创建请求中直接携带经过校验和稳定排序的 SubENI 标签，不再依赖区域受限的后置标签接口。

### 15. Security Group 候选和数量边界不完整

* **实际测试问题场景：** 安全组列表含空值、重复项、跨 VPC 项或超过云侧附加上限时，错误可能延迟到云 API 才暴露。
* **根因：** 显式 ID、标签选择和 trunk 继承三条路径没有共用同一套校验。
* **解决方案：** 对三条路径统一执行去重、非空、VPC 归属和数量上限检查，并在创建请求前失败。

### 16. 未知 Flavor 的容量处理可能过度乐观

* **实际测试问题场景：** 动态查询失败且静态表没有该机型时，系统仍可能推测一个可分配容量。
* **根因：** 容量缺失时采用了兜底值，无法区分“容量为零”和“容量未知”。
* **解决方案：** 未知容量按不可分配处理；动态查询成功后再更新限制，不猜测云侧上限。

### 17. 已删除 SubENI 的 404 没有完成本地收敛

* **实际测试问题场景：** 云端资源已不存在，但删除接口返回 404 后，本地 inventory 和节点状态仍保留旧记录，后续删除也被中断。
* **根因：** Release 流程把 `NotFound` 当成普通失败立即返回。
* **解决方案：** 仅将明确的 `NotFound` 视为云端已收敛，清理本地状态并继续处理同批次后续资源；其他错误保持失败语义。

### 18. CiliumNode 被删除后不会自动重建

* **实际测试问题场景：** Agent 仍在运行时删除本节点 CiliumNode，对象不会自行恢复。
* **根因：** CiliumNode 只在 Agent 启动和节点发现阶段创建，没有运行期存在性检查。
* **解决方案：** 增加低频、只读的本地 CiliumNode 存在性控制器，仅在对象确实不存在时触发重建。

### 19. 云 API 配置缺少路径和格式校验

* **实际测试问题场景：** Endpoint、Region 或 Project ID 包含非法 scheme、凭据、路径分隔符或控制字符时，错误会进入 SDK 请求阶段。
* **根因：** 客户端构造前只检查非空，没有限制 URL 和路径字段的语法边界。
* **解决方案：** 客户端初始化时校验 HTTP(S) Endpoint、主机、Region 和 Project ID，拒绝嵌入凭据、query、fragment 和路径注入字符。

### 20. API 限流等待没有正确响应 Context 取消

* **实际测试问题场景：** 请求在限流队列中已经超时，仍可能继续等待并发起无主云请求。
* **根因：** 限流器进行了重复 reservation，且调用链没有统一传播等待错误。
* **解决方案：** 每次调用只创建一个可取消 reservation；等待结束后先检查 Context，再决定是否发起 HTTP 请求。

### 21. HuaweiCloud IPAM 未明确拒绝 IPv6

* **实际测试问题场景：** HuaweiCloud IPAM 与 IPv6 同时启用时，配置校验可能放行到运行时初始化。
* **根因：** IPv6/IPAM 兼容性检查只覆盖了既有 ENI 模式，没有包含 HuaweiCloud 模式。
* **解决方案：** 在统一配置校验中明确拒绝 HuaweiCloud IPAM 与 IPv6 组合，同时保持其他支持 IPv6 的 IPAM 模式不变。

### 22. 外部 Secret 和空凭据边界不足

* **实际测试问题场景：** Secret 名为空、只有空格、格式非法，或键存在但值为空时，错误出现得过晚。
* **根因：** Helm 只判断字段存在，Operator 客户端构造也没有统一 trim 和非空校验。
* **解决方案：** Helm 校验 Secret 名的 DNS 规则；Operator 在创建 SDK 客户端前校验 AK、SK 和 Project ID 的有效非空值。

## 三、并发调谐与状态一致性

### 23. Resync 覆盖刚创建或刚释放的 SubENI

* **实际测试问题场景：** 全量 Resync 与创建、释放并发时，旧快照可能覆盖新建资源，或重新发布已经释放的资源。
* **根因：** 快照读取和 inventory 更新没有使用同一把串行化锁。
* **解决方案：** 创建、释放和全量 Resync 共用 resync 锁；释放成功后同步删除 manager inventory 中的记录。

### 24. 旧 Delete 事件删除新的 Endpoint 绑定

* **实际测试问题场景：** Pod IP 快速复用时，旧 Endpoint 的 Delete 事件晚于新 Endpoint 的 Ready 事件到达，新的 BPF 映射被误删。
* **根因：** 删除路径只按 IP 查找，没有核对 VLAN、MAC、ifindex 和 Endpoint ID 的完整 owner。
* **解决方案：** 删除前同时核对 source map 和 ingress map 的 owner；任何字段不匹配都保留当前绑定。

### 25. Node 更新期间混用两个 Revision

* **实际测试问题场景：** `UpdatedNode` 与创建并发时，limit 查询、云请求和 inventory 归属可能分别使用不同的 instance、subnet 或安全组配置。
* **根因：** Node spec 和 instance ID 在多个锁窗口中分别读取。
* **解决方案：** 在一次锁内快照完整 Node revision，并让该快照贯穿容量计算、云请求和本地归属更新。

### 26. 同一节点可能被并发重复扩容

* **实际测试问题场景：** 两个调谐循环携带相同旧 allocation action 时，会同时创建 SubENI，突破期望水位。
* **根因：** 节点扩容缺少串行化，创建前也没有依据最新 inventory 重新收紧请求量。
* **解决方案：** 引入节点级 create lock，并在持锁期间按当前资源数量重新计算实际可创建数量。

### 27. 运行期切换子网或安全组可能残留旧配置

* **实际测试问题场景：** 配置从显式子网切换到标签选择，或从一个安全组切换到另一个安全组后，新请求仍可能带入旧值。
* **根因：** 调谐路径复用了上一个 Node revision 的派生缓存。
* **解决方案：** 每次创建都从当前 Node 快照重新解析 subnet 和 Security Group，不跨 revision 复用候选集合。

## 四、策略路由、邻居与 BPF Map

### 28. Endpoint 恢复早于 BPF Map 创建

* **实际测试问题场景：** 节点重启后，Endpoint 恢复事件先执行，但 HuaweiCloud BPF map 尚未创建；节点表面就绪，映射实际缺失。
* **根因：** Endpoint subscriber 注册顺序早于 map 的创建、pin 和 typed open。
* **解决方案：** 在注册 Endpoint 事件前按固定 ABI 创建并 pin 两张 map，再通过 typed map spec 打开。

### 29. 启动时缺少 BPF Map 的事务化对账

* **实际测试问题场景：** Map 中存在旧 owner、缺失新 owner或两张 map 不一致时，单次更新失败会留下半完成状态。
* **根因：** 启动恢复逐条修改 live map，没有完整 desired snapshot 和回滚机制。
* **解决方案：** 先构建唯一 owner 的期望快照，按依赖顺序更新和删除；任一步失败都恢复两张 map 的原始快照。

### 30. 策略路由安装失败会留下部分状态

* **实际测试问题场景：** Rule 已创建但 nexthop/default route 失败，或 stale rule 清理中途失败时，节点残留不完整路由。
* **根因：** Rule、route 和 stale 清理是独立操作，没有统一事务边界。
* **解决方案：** 串行化路由配置，记录新增、替换和删除项；失败时按相反顺序回滚，并恢复原有 route/rule。

### 31. 合法的嵌套 CIDR 被误判为冲突

* **实际测试问题场景：** SubENI 子网是 VPC CIDR 的合法子集，却被严格 overlap 校验拒绝。
* **根因：** 校验逻辑把“包含关系”和“完全重复”都当作不可接受冲突。
* **解决方案：** 只拒绝完全重复或语义冲突的 CIDR，允许 VPC 与子网的正常嵌套关系。

### 32. VLAN 复用时残留旧网关路由

* **实际测试问题场景：** Pod 删除后专用表保留，随后同一 VLAN 被分配给不同网关，新旧 nexthop 同时存在。
* **根因：** 无活跃 rule 时没有回收完整的 default route 和 nexthop route 对。
* **解决方案：** 将旧 route 回收纳入现有路由事务；新 nexthop 安装后再删除旧项，后续失败时恢复原状态。

### 33. Gateway 邻居可能覆盖其他组件的永久项

* **实际测试问题场景：** 同一接口已有相同 gateway IP、不同 MAC 的 permanent neighbor 时，Agent 直接覆盖该项并继续发布 BPF 映射。
* **根因：** 邻居处理无条件执行 `NeighSet`，没有校验既有 owner 和冲突范围。
* **解决方案：** 串行化邻居调谐，接受完全一致项；同接口同 gateway 不同 MAC 时 fail closed，且不更新 BPF map 和缓存。

### 34. Trunk Link Flap 后路由和邻居静默缺失

* **实际测试问题场景：** Trunk down/up 后主路由仍可让部分流量通过，但 endpoint 专用表和永久邻居没有恢复。
* **根因：** 运行期只依赖启动配置和 Endpoint 事件，没有非侵入式周期对账；旧恢复路径还会提前拉起被人为关闭的接口。
* **解决方案：** 增加不修改 MTU 和 link 管理状态的周期 `Reconcile`，先恢复永久邻居，再恢复 router 与普通 Endpoint 路由。

### 35. 旧共享路由规则会干扰新专用表

* **实际测试问题场景：** 从共享 trunk 表升级后，旧规则优先级仍高于新规则，流量继续命中旧路径。
* **根因：** 新表安装完成后没有事务化清理相同 Pod IP 的历史规则。
* **解决方案：** 先完整安装新 rule/route，再删除精确匹配的遗留规则；清理失败时恢复旧规则并回滚新路径。

## 五、VLAN 数据面边界

### 36. 线内 VLAN 与 skb Metadata 同时存在时重复处理

* **实际测试问题场景：** 报文同时携带线内 VLAN header 和 skb VLAN metadata 时，可能重复 pop 或在后续通用 VLAN 检查中被丢弃。
* **根因：** HuaweiCloud 处理完成后仍保留旧的 `vlan_present` 状态，未标记本次报文已经完成处理。
* **解决方案：** 统一解析两种 VLAN 表示，校验 ID 一致性，并在成功处理后更新状态，避免重复消费。

### 37. 未知 VLAN 的默认拒绝边界不明确

* **实际测试问题场景：** Trunk 上收到未知 VLAN、未知 MAC 或不完整 header 时，旧路径可能继续进入通用数据面。
* **根因：** Map miss、解析失败和非目标协议共用模糊返回值。
* **解决方案：** 区分 trunk 与非 trunk；trunk 上的未知或非法 SubENI VLAN 明确 fail closed，非 trunk 流量直接旁路 HuaweiCloud 逻辑。

### 38. VLAN Pop Helper 失败被误报为成功

* **实际测试问题场景：** `skb_vlan_pop` 失败时，函数仍可能返回已处理状态，使损坏报文继续向后执行。
* **根因：** Helper 返回值没有转换为专用 drop reason。
* **解决方案：** 检查 helper 返回值，失败时返回 HuaweiCloud 专用 drop code，不修改 owner map 或报文状态。

### 39. BPF Map 容量边界缺少明确约束

* **实际测试问题场景：** Map 接近或达到容量上限时，更新失败容易被误认为 Endpoint 逻辑错误。
* **根因：** 测试没有按生产 map schema 和真实 `max_entries` 覆盖容量边界。
* **解决方案：** 使用生产 MapSpec 构造隔离 map，覆盖接近上限、达到上限和删除后复用场景，并保留原始更新错误。

## 六、测试工具和证据判定问题

### 40. 对未约定源地址的场景进行了额外断言

* **实际测试问题场景：** 直连 Pod 场景只要求连通性，测试脚本却额外校验源地址，产生误报。
* **根因：** 通用源地址检查被套用到所有数据面场景，没有以客户用例中的断言字段为准。
* **解决方案：** 只对明确约定源地址的场景执行抓包断言，其他场景只检查其定义的连接语义。

### 41. tcpdump 尚未开始监听就发送请求

* **实际测试问题场景：** 业务请求成功，但抓包进程启动较慢，未捕获到目标 SYN，导致证据不完整。
* **根因：** 脚本用固定等待时间代替抓包就绪信号。
* **解决方案：** 等待 tcpdump 输出明确的 listening 状态后再发送请求，并设置有界的捕获超时。

### 42. Shell 管道掩盖真实退出码

* **实际测试问题场景：** 测试命令失败，但外层 `tee` 返回成功，清理或判定逻辑把该次执行当成有效记录。
* **根因：** 脚本未启用 `pipefail`，也没有读取管道中目标命令的退出码。
* **解决方案：** 所有执行脚本使用 `set -euo pipefail`，并由统一清理 trap 恢复临时配置。

### 43. Distroless 镜像中执行不存在的诊断命令

* **实际测试问题场景：** 测试脚本进入 Operator 容器执行 `cat`、Shell 等工具，命令本身不存在。
* **根因：** 把运行时镜像当作完整调试环境使用。
* **解决方案：** 通过 Kubernetes API、挂载文件或独立诊断容器读取状态，不假设 distroless 镜像包含 Shell 工具。

### 44. 配置投射尚未更新就重启组件

* **实际测试问题场景：** ConfigMap 已修改，但 Pod 内 projected volume 仍是旧值，立即重启后验证到错误配置。
* **根因：** 脚本只等待 API 对象更新，没有等待目标节点上的投射文件同步。
* **解决方案：** 重启前轮询目标 Pod 的投射文件或 checksum，确认新配置已经落地。

### 45. 云配额错误掩盖目标参数校验

* **实际测试问题场景：** 验证非法 Security Group 时，资源已达到配额上限，云 API 先返回配额错误，未进入目标参数校验。
* **根因：** 故障注入前没有确认请求能越过容量前置检查。
* **解决方案：** 使用隔离资源并预留一个创建名额；先校验基线容量，再注入目标错误，结束后恢复资源。

### 46. BPF 容量测试沿用了不适用的 Pin 配置

* **实际测试问题场景：** 隔离容量测试在真正插入数据前，因为缺少 pin path 而初始化失败。
* **根因：** 测试直接复用了带 `LIBBPF_PIN_BY_NAME` 的生产 MapSpec。
* **解决方案：** 复制生产 schema、类型和容量，仅在临时测试实例中关闭 pinning，避免改变被测边界。

## 七、修复归属

* 云 API、IPAM、Operator、CiliumNode、配置和部署相关实现归入
  `0001-huaweicloud-control-plane.patch`。
* Daemon/CNI 运行时、路由、BPF、VLAN、Endpoint 和 Service 数据路径实现归入
  `0002-huaweicloud-data-plane.patch`。
* 单元、边界、并发、故障注入、BPF、特权路由和实机辅助测试归入
  `0003-huaweicloud-tests.patch`；测试工具修正不改变产品代码语义。
